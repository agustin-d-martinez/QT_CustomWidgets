{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run test/test.py",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/test/test.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
